<%= form_with(model: category_type, local: true) do |form| %>
  <% if category_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_type.errors.count, "error") %> prohibited this category_type from being saved:</h2>

      <ul>
      <% category_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_type_name %>
    <%= form.text_field :category_type_name, id: :category_type_category_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
